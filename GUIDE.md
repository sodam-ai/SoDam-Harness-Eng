# SoDamHarness 사용 설명서 (GUIDE) — 완전 초보자용

> **SoDamHarness(소담하네스)** 는 **AI(Claude Code)로 코딩할 때 실수로 큰 사고가 나지 않게 막아 주는 "안전벨트"** 입니다.
> 개발도, AI도, 컴퓨터도 처음인 분이 **그대로 따라 하기만 해도** 되도록 만들었습니다.
>
> ⚠️ **가장 먼저 알아 둘 것 (정직한 약속):** 이 도구는 **흔한 위험을 막아 주는 "안전벨트"** 이지, 모든 사고를 막는 **"방탄막"이 아닙니다.** "100% 안전"은 **세상에 없습니다.** 그래도 안전벨트는 사고 피해를 크게 줄여 줍니다.

---

## 0. 이게 뭔가요? (1분 이해)

AI에게 "이거 해 줘"라고 시키면 AI가 내 컴퓨터의 파일을 만들고·고치고·지웁니다. 편하지만, **AI가 실수로 중요한 파일이나 폴더를 지워 버리면** 되돌리기 어렵습니다.

SoDamHarness는 그 사이에서 **문지기** 역할을 합니다.

- 🛑 **아주 위험한 일**(폴더 통째 삭제 등)은 **아예 막습니다.**
- 💾 **되돌리기 어려운 일**(파일 삭제·덮어쓰기) 전에는 **자동으로 백업**하고 **"정말 할까요?"** 라고 물어봅니다.
- ↩️ 실수했어도 **백업에서 되살릴 수 있습니다.**
- 🗣️ 모든 안내를 **쉬운 한국어**로 해 줍니다.

---

## 1. 사전 준비물 (필요한 프로그램)

딱 **2가지**만 있으면 됩니다. 둘 다 무료입니다.

### (1) Node.js — 안전벨트가 동작하는 데 필요한 엔진
- **확인 방법:** 검은 명령창(아래 1-A 참고)에 `node --version` 입력 → `v18` 이상 숫자가 나오면 이미 설치된 것(예: `v22.19.0`).
- **없으면 설치:**
  1. 웹브라우저에서 **https://nodejs.org** 접속.
  2. **"LTS"** 라고 적힌 큰 버튼을 클릭해 설치 파일을 내려받습니다. (LTS = 안정 버전)
  3. 내려받은 파일을 더블클릭 → **"다음(Next)"** 만 계속 눌러 설치 → 컴퓨터/터미널을 껐다 켭니다.

### (2) Claude Code — AI 코딩 도구 (이 안전벨트가 붙는 곳)
- 이미 이 글을 Claude Code에서 보고 있다면 **설치되어 있는 것**입니다.
- 없으면 공식 안내(https://claude.com/claude-code)를 따라 설치하세요.

> 💡 **"검은 명령창(터미널)"이 뭔가요?** 컴퓨터에 글자로 명령을 내리는 창입니다.
> **여는 법(윈도우):** 키보드 **⊞ 윈도우키** → `powershell` 입력 → **Windows PowerShell** 클릭.

---

## 2. 설치 방법 (단계별)

설치는 **딱 한 번만** 하면 됩니다. 두 가지 방법 중 **하나**를 고르세요.

### 방법 A — `/plugin` 화면으로 설치 (초보자 추천 · 화면 보고 클릭)

1. Claude Code 입력칸에 **`/plugin`** 을 치고 Enter → 플러그인 관리 화면이 열립니다.
2. **마켓플레이스 추가(Add marketplace)** 를 고른 뒤, 아래 중 하나를 입력합니다.
   - **공개된 경우(권장):** `sodam-ai/SoDam-Harness-Eng`
   - **내 컴퓨터의 폴더로 설치(개발/지인 공유):** 그 폴더의 전체 경로
     (예: `D:\AI_Dev_Work\2026y\26y_06m_22d_SoDam-Harness-Eng`)
3. 목록에서 **`sodam-harness`** 를 골라 **설치(Install)** 합니다.
4. **Claude Code를 완전히 껐다 켭니다.** (`/exit` 입력 → 다시 `claude` 실행, 또는 터미널 창을 닫고 새로 열기)
5. 입력칸에 **`/sodam-harness`** 를 쳐서 **명령 8개**(아래 6번 표)가 보이면 **설치 성공**입니다.

### 방법 B — 명령으로 설치 (검은 명령창)

검은 명령창에 아래를 **한 줄씩** 입력합니다.

```
claude plugin marketplace add "D:\AI_Dev_Work\2026y\26y_06m_22d_SoDam-Harness-Eng"
```
```
claude plugin install sodam-harness@sodamharness-marketplace
```

그다음 Claude Code를 **완전히 껐다 켜고**, `/sodam-harness` 로 확인합니다.

> 📌 **중요(현재 기준):** 아직 GitHub에 공개되지 않았다면 **방법 A의 "공개된 경우"는 동작하지 않습니다.**
> 지금은 **내 컴퓨터 폴더 경로(방법 A의 두 번째 / 방법 B)** 로 설치하세요. 공개되면 GitHub 방식이 가장 간편합니다.

---

## 3. 빠른 시작 (3분)

1. (설치 후) Claude Code를 **새로** 켭니다.
2. 입력칸에 **`/sodam-harness-install`** → "안전벨트가 켜졌어요" 안내가 나옵니다.
3. **버려도 되는 빈 연습 폴더**에서 AI에게 평소처럼 일을 시켜 보세요.
   - 예: "메모.txt 파일 만들어 줘" → 그냥 만들어집니다.
   - 예: "이 폴더 통째로 지워 줘" → **막힙니다** 🛑 (안전벨트 작동!)
4. 궁금하면 **`/sodam-harness-status`** (지금 상태), **`/sodam-harness-log`** (방금 뭐 했는지).

---

## 4. 사용·작동 방법 (안전벨트가 언제 어떻게 작동하나)

평소엔 **평소처럼** AI에게 일을 시키면 됩니다. 위험한 순간에만 안전벨트가 나섭니다.

| AI가 하려는 일 | 안전벨트의 반응 |
|---|---|
| 파일 **만들기**, 읽기, 일반 편집 | 그냥 진행 (안 막음) |
| **폴더 통째로 삭제**, 시스템 위험 명령 | 🛑 **차단** (아예 못 하게 막음) |
| **파일 삭제 / 덮어쓰기** | 💾 먼저 **자동 백업** → "정말 할까요?" **확인** |
| 배포·외부 전송 등 되돌리기 어려운 일 | ⚠️ **확인** 요청 |
| 비밀번호·토큰·인증 파일 | 🔒 **건드리지 않음** (백업에도 저장 안 함) |

- **백업**은 내 컴퓨터 안 `~/.sodamharness/backups/` 에 자동 저장됩니다. (아래 8번)
- 실수로 지웠다면 **`/sodam-harness-undo`** 로 백업에서 골라 **되살립니다.**
- ⚠️ **자동 승인(YOLO) 모드**를 켜 두면 "정말 할까요?" 질문이 생략될 수 있습니다. 그래도 **백업은 됩니다.** 질문을 꼭 보려면 **기본 모드**로 쓰세요.

---

## 5. 워크플로우 (일상 사용 흐름)

```
[평소] AI에게 일 시키기
        │
        ▼
[안전벨트가 자동 점검]
        │
        ├─ 안전한 일      → 그냥 진행 ✅
        ├─ 위험한 일      → 백업 💾 + "정말 할까요?" 확인 ⚠️
        └─ 아주 위험한 일 → 차단 🛑 (안 함)
        │
        ▼
[실수했다?] → /sodam-harness-undo 로 되살리기 ↩️
[뭐 했지?]  → /sodam-harness-log 로 확인 📜
[잘 되나?]  → /sodam-harness-status 로 점검 🩺
```

---

## 5-1. (선택) Codex(코덱스)에서도 쓰기

> ⚠️ **정직한 한계 먼저:** SoDamHarness의 **자동 차단·백업·되돌리기(undo)는 Claude Code 전용**이라 **Codex에선 동작하지 않습니다.** Codex에선 **Codex 자체 기능(승인 창 + 샌드박스)** 으로 대신하며, 보호 방식·강도가 다릅니다. ("100% 안전"은 없습니다.)

1. **쉬운 말투** — 플러그인 폴더의 **`AGENTS.md`** 를 Codex로 작업할 프로젝트 폴더에 **복사**합니다. (기존 `AGENTS.md`가 있으면 덮어쓰지 말고 내용을 **추가/병합**.)
2. **보수적 안전 설정** — 템플릿 **`codex/config.toml.example`** 의 두 줄(`approval_policy`, `sandbox_mode`)을 **`~/.codex/config.toml`** 에 **추가/병합**합니다.
   - ⚠️ 기존 설정 **덮어쓰기 금지** · **`~/.codex/auth.json`(로그인 토큰)은 절대 건드리지 않기** · 적용은 **본인이 직접**.
   - 값 이름은 Codex 버전마다 다를 수 있어요 → 공식 문서: https://developers.openai.com/codex/config-reference
3. **확인** — Codex를 다시 켜고 **버려도 되는 빈 폴더**에서 위험 작업을 시켜 **승인 창이 뜨는지** 확인합니다.
4. 자세한 단계는 플러그인 폴더의 **`codex/CODEX_SETUP.md`** 에도 있습니다. 막히면 (Claude Code에서) **"코덱스 안전 설정 도와줘"** 라고 하세요.

---

## 6. 명령어 (언제 무엇을 쓰나)

입력칸에 **`/sodam-harness`** 까지 치면 아래가 자동완성으로 뜹니다. 모두 **Claude Code 전용**입니다.

### 명령 6개

| 명령어 | 언제 쓰나 | 무슨 일이 일어나나 |
|---|---|---|
| `/sodam-harness-install` | 설치 직후 | 설치 확인 + 시작 안내 |
| `/sodam-harness-status` | 잘 되는지 궁금할 때 | 상태 점검 + "다음에 할 일" 안내 |
| `/sodam-harness-fix` | 뭔가 안 될 때 | 증상별 해결 방법 안내 |
| `/sodam-harness-undo` | 실수로 지웠을 때 | 백업 목록에서 **골라** 복구 |
| `/sodam-harness-trust` | 같은 작업 그만 묻게 | "이 폴더 이 작업 안 물어봐도 돼" — 이번 세션 동안 안 묻기 (완전 차단·백업은 유지) |
| `/sodam-harness-log` | 방금 뭐 했나 궁금할 때 | AI가 한 일 타임라인 (파일명·시각만, 비밀은 가림) |

### 스킬 2개 (자동으로 도와주는 기능)

| 이름 | 하는 일 |
|---|---|
| `/sodam-harness-beginner-tone` | 모든 안내를 **쉬운 한국어 말투**로. (보통 자동 적용 — 어려우면 "쉽게 설명해 줘"라고 한마디) |
| `/sodam-harness-self-check` | AI가 "다 됐어요" 하기 전에 **진짜 됐는지 점검**하고 증거를 보여주게 함. |

> 💡 위 명령은 모두 **Claude Code 전용**입니다. Codex에서 쓰는 법은 위 **"5-1. (선택) Codex에서도 쓰기"** 를 보세요.

---

## 7. 파일·문서 위치

- **백업 폴더(자동 생성)** — 내 컴퓨터의 홈 폴더 안 `.sodamharness/backups/`
  - 윈도우 예: `C:\Users\<내이름>\.sodamharness\backups\`
  - 맥 예: `/Users/<내이름>/.sodamharness/backups/`
  - 이 폴더엔 **비밀번호·토큰을 저장하지 않습니다.** 백업 파일만 들어가요.
- **활동 기록(자동)** — 같은 `.sodamharness/` 폴더 안. `/sodam-harness-log` 로 봅니다. (파일명·시각만, 비밀은 가림)
- **문서 위치(플러그인 폴더 안)**
  - `README.md` / `README.en.md` — 짧은 소개 (한/영)
  - `GUIDE.md` / `GUIDE.en.md` — 이 상세 사용설명서 (한/영)
  - `TESTING.md` / `TESTING.en.md` — 테스트(검증) 방법 (한/영)
  - `codex/config.toml.example`, `codex/CODEX_SETUP.md` — 코덱스 설정 자료
  - `LICENSE` / `NOTICE` — 라이선스·고지
- 같은 내용의 **PDF**: `README.pdf` · `README.en.pdf` · `GUIDE.pdf` · `GUIDE.en.pdf`

---

## 8. 문제·오류 대처 (증상 → 왜 → 지금 할 것)

| 증상 | 왜 그런가 | 지금 할 것 |
|---|---|---|
| `/sodam-harness-...` 명령이 **안 떠요** | 설치 후 **재시작을 안 함**, 또는 설치가 안 됨 | Claude Code를 **완전히 껐다 켜기** → `/sodam-harness` 확인. 그래도면 2번(설치) 다시 |
| 위험 작업인데 **안 멈춰요** | 플러그인이 꺼졌거나, 우리가 모르는 위험 패턴 | Claude Code 껐다 켜기 → `/sodam-harness-status` |
| 너무 **자주 물어봐요** | 안전 위주 기본값 | 같은 작업이면 `/sodam-harness-trust` 로 이번 세션 동안 그만 묻기 |
| **"Node.js 없음"** 이라고 떠요 | 안전 훅이 Node.js로 동작 | https://nodejs.org 에서 **LTS** 설치 후 재시작 (위 1번) |
| 실행 차단 경고(SmartScreen/Gatekeeper) | 처음 보는 프로그램이라 OS가 한 번 물음 | 공식 경로면 "실행 허용" |
| 실수로 **지웠어요** | — | `/sodam-harness-undo` (목록에서 고르기) |
| 백업 폴더 오류(권한/용량) | 디스크 가득참·권한 문제 | 공간 확보 후 재시도 (백업 못 뜨면 위험 작업은 **자동 중단**) |
| 한글이 깨져요 | 인코딩 | 보통 안 깨짐(Node.js). 그래도면 `/sodam-harness-fix` |
| 설명이 어려워요(말투) | 쉬운 말투는 Skill로 전달돼 **항상 자동은 아닐 수 있음** | AI에게 **"쉽게 설명해 줘"** 라고 한마디 |
| 옛 이름(`/install` 등)·중복이 떠요 | 옛 설치 잔재 | Claude Code 완전 재시작. 그래도면 재설치 |

> 더 자세한 진단은 입력칸에 **`/sodam-harness-fix`**.

---

## 9. 제거(삭제) 방법

1. Claude Code에서 **`/plugin`** → **SoDamHarness 제거(Uninstall)**.
   - 또는 검은 명령창에 `claude plugin uninstall sodam-harness@sodamharness-marketplace`.
2. 백업까지 지우려면 홈 폴더의 **`.sodamharness/` 폴더를 삭제**합니다. (백업을 남기고 싶으면 그대로 두세요.)

---

## 10. 자주 묻는 질문 (FAQ)

- **Q. 정말 안전한가요?** — 흔한 위험은 막아주지만 **모든 경우를 100% 막진 못합니다.** "안전벨트"이지 "방탄막"은 아니에요.
- **Q. 제 비밀번호·토큰을 보나요?** — **아니요.** 토큰·인증 파일에 접근하지 않고, 데이터를 밖으로 보내지 않습니다. 백업에도 비밀 파일은 **제외**합니다.
- **Q. 인터넷이 필요한가요?** — 설치할 때만요. 안전 기능 자체는 인터넷 없이 내 컴퓨터 안에서 돕니다.
- **Q. 실수로 폴더를 지웠어요.** — 폴더는 통째 백업이 안 돼서 **삭제 자체가 막힙니다.** 파일은 `/sodam-harness-undo` 로 되살릴 수 있어요.
- **Q. 맥(Mac)에서도 되나요?** — 같은 코드로 동작하도록 만들었지만, 현재 **윈도우에서만 실제 확인**했습니다(맥은 미검증). 맥에서 쓰면 알려 주세요.
- **Q. Codex에서도 똑같이 보호되나요?** — **아니요.** 차단·백업·되돌리기는 **Claude Code 전용**입니다. Codex는 자체 승인·샌드박스로 대신합니다(위 5-1).

---

## 11. 라이선스 · 저작권 · 상업적 사용 (엄격 고지)

> ⚖️ **이 문서는 법률 자문이 아닙니다.** 아래는 안전한 사용·배포를 위한 안내입니다. 최종 판단은 본인 책임이며, 필요 시 전문가의 검토를 받으세요.

- **라이선스: Apache License 2.0** · **저작권: © 2026 SoDam AI Studio.**
- **할 수 있는 것 (Apache-2.0 허용)**: 수정 · 복제 · 포크 · 재배포 · **상업적 사용 · 판매 · 서비스 운영 · 교육 자료 · 회사/고객사 납품** · 특허 사용.
- **지켜야 할 의무**: **라이선스·저작권 고지 보존** · **변경한 부분 명시** · **NOTICE 파일 동봉**(있을 경우).
- **제공되지 않는 것**: **보증 없음(AS-IS)** · **상표권은 부여되지 않음.**
- **타사 상표**: "Claude", "Claude Code", "Codex", "Anthropic", "OpenAI", "Node.js" 등은 각 소유자의 상표입니다. 본 제품은 이들과 **공식 제휴·보증 관계가 없습니다("not affiliated / not endorsed").** 호환·연동 사실의 **명목적 표시**로만 언급합니다.
- **책임 제한 · 면책**: 본 소프트웨어는 **"있는 그대로(AS-IS)"** 제공되며, 사용으로 인한 **모든 결과의 책임은 사용자에게** 있습니다. **"100% 사고 방지"나 "법적으로 100% 안전"은 보장하지 않습니다.**
- **데이터·백업 면책**: 백업·되돌리기는 **보조 수단**입니다. 디스크 고장·용량 부족·권한 문제 등으로 **백업이 실패할 수 있으며**, 데이터 손실의 책임은 사용자에게 있습니다. 중요한 자료는 **별도 백업**을 권장합니다.
- **사용자가 별도로 확인할 것**: **AI 모델 사용 약관(Anthropic/OpenAI 등)** · **API 요금제** · **외부 서비스 약관** · (사용 시) 폰트·이미지·아이콘 라이선스. 이는 **본 라이선스 범위 밖**입니다.
- **외부 자료 주의**: 코드·이미지·폰트·샘플 등 외부 자료를 추가할 땐 **권리 관계를 먼저 확인**하고, **AGPL/GPL 등 양립 불가 라이선스는 차용하지 않습니다.**
- 자세한 전문은 **[LICENSE](./LICENSE)**, 고지는 **[NOTICE](./NOTICE)** 참고.

---

## 12. 안전·한계 (정직 고지)

- 이 도구는 **흔한 위험을 줄이는 보조 안전장치**입니다. **완벽하지 않습니다.**
- **새롭거나 교묘한 위험 패턴은 놓칠 수 있습니다.** 위험한 작업 전엔 항상 한 번 더 생각하세요.
- **윈도우에서 실제 검증**했고, **맥은 미검증**입니다.
- 진짜로 중요한 자료는 **이 도구만 믿지 말고** 따로 백업하세요.

---

*이 문서(GUIDE.md)와 PDF(GUIDE.pdf)는 내용이 동일합니다. 영문판: README.en.md / GUIDE.en.md.*

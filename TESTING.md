# SoDamHarness 테스트 가이드 (베타 검증용) 🧪

> English: [TESTING.en.md](./TESTING.en.md) · 사용 설명서: [README.md](./README.md)
> 이 문서대로 따라 하면 **안전벨트가 진짜 작동하는지** 확인할 수 있어요. 개발을 몰라도 됩니다.
> ⚠️ 테스트는 **반드시 "버려도 되는 빈 연습 폴더"** 에서만 하세요(일부러 파일을 지워보는 과정이 있어요).

---

## 0. 준비물
- **Node.js (18 이상)** 설치 — 없으면 https://nodejs.org 에서 **LTS**.
- **Claude Code** 설치·로그인.
- SoDamHarness 설치 후 **Claude Code를 한 번 껐다 켜기**(안전벨트는 켤 때 장착돼요).

## 1. 설치 확인
입력: `/sodam-harness:install`
- ✅ 합격: "안전벨트가 켜졌어요" 안내 + Node.js 버전이 나옴.

## 2. 연습 재료 만들기 (이게 첫 합격 = "안전한 일은 안 막힘")
버리는 빈 폴더를 하나 만들고 그 안에서 Claude Code를 연 뒤, 입력:
```
여기에 메모.txt 파일과 '버릴폴더'라는 빈 하위 폴더를 만들어줘.
```
- ✅ 합격: 막힘 없이 만들어짐.

## 3. 핵심 기능 5가지 (각각 "합격 신호" 확인)
| # | 시킬 말 | 합격 신호 |
|---|---|---|
| ① 폴더 삭제 | "버릴폴더 통째로 지워줘" | **"폴더 통째 삭제는 막았어요" 라며 차단** → 폴더 살아있음 |
| ② 파일 삭제 | "메모.txt 지워줘" | 지우기 직전 **자동 백업**(아래 ⚠️ 참고) |
| ③ 되돌리기 | `/sodam-harness:undo` | **백업 목록에서 골라**(예: "방금/3분 전") 복구 → 메모.txt 다시 생김 |
| ④ 안전 작업 | "새글.txt 만들어줘" | 막힘 없이 생성(과잉차단 없음) |
| ⑤ 덮어쓰기 | "메모.txt 내용을 'B'로 바꿔(덮어써) 줘" | 바꾸기 직전 **자동 백업** + 확인(아래 ⚠️ 참고) → `/sodam-harness:undo`로 **이전 내용 복구** |

> ⚠️ **자동 승인 모드**로 쓰면 ②(파일 삭제)·⑤(덮어쓰기)는 묻지 않고 바로 진행될 수 있어요. 정상입니다 — **백업+되돌리기(③)로 보호**됩니다. 확인 창을 꼭 보려면 Claude Code를 **기본 모드**로 쓰세요.

## 4. 환경별 확인
- **Windows / Mac 각각** 0~3 단계를 반복.
- **다른 PC(처음 설치)** 에서 마켓플레이스로 설치 → 0~3 반복(초기 설치 흐름 점검).

## 5. 초보자 베타 (가장 중요)
- 초보자 **1~3명**에게 **`README.md`만** 건네고 옆에서 관찰:
  설치 → "파일 하나 만들어줘" → "지워줘" → `/sodam-harness:undo` 까지 **혼자 도달하는가?**
- **막힌 지점 = 문서를 고칠 목록**(어디서 헷갈렸는지 적어두기).

## 6. 결과 기록 (이렇게 남겨 주세요)
| 항목 | 합격/실패 | 화면에 뜬 글자 |
|---|---|---|
| 1 설치 | | |
| 3-① 폴더 차단 | | |
| 3-② 파일 백업 | | |
| 3-③ 되돌리기 | | |
| 3-④ 안전 작업 | | |
| 3-⑤ 덮어쓰기 백업 | | |
- **실패 시**: 그 화면의 글자(또는 스크린샷)를 만든 사람에게 전달 → 바로 고칠 수 있어요.

## 7. 정직한 한계
- "100% 사고 방지"는 보장하지 않아요(흔한 위험만 막음, 참고용·사용자 책임).
- 폴더는 통째 백업이 안 돼서 **삭제 자체를 막습니다**(빈 폴더도 막힘 — 의도된 동작, 탐색기로 지우면 됨).

---
*SoDamHarness — SoDam AI Studio · Phase 1 (MVP) 베타 검증 가이드*

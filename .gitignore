# SoDamHarness — Git 추적 제외 (보안: 비밀값·사용자 데이터 보호)

# 의존성 설치 폴더
node_modules/

# 로컬 설정·백업 (사용자 파일·잠재적 민감정보 — 절대 커밋 금지)
.sodamharness/

# 다른 플러그인 작업 폴더·산출물(이 저장소와 무관 — 공개 시 제외)
.omc/
.todos-report.md
.complexity-log.md
.remember/
.pair-programming-session.md
.plugin-config/
# 2026-08-21 추가: 이 코드가 만든 파일이 아님(hooks/*.mjs 전수 검색 결과 0건) — 다른 플러그인이
# 이 폴더를 작업 디렉터리로 쓰며 남긴 부산물로 추정. git add -A 실수 시 혼입 방지 목적.
.failure-tracker.jsonl
.sodam-re/

# 테스트 임시 산출물
*.tmp

# OS·에디터 부산물
.DS_Store
Thumbs.db
*.log

# 기획 문서·로컬 메모 (사용자 지정 — 공개 저장소에 절대 push 금지)
/.PRD/
/.이 문서는 반드시 깃허브 저장소에 푸시 하지 않기.md

# PDF 생성 중간 파일 (pandoc → Chrome 헤드리스 변환 시 자동 생성)
*.html
# 단, 아래 2개는 README의 정식 HTML 산출물(MD와 동일 내용) — 추적함
# (GUIDE.html·GUIDE.en.html은 2026-07-27 GUIDE.md/GUIDE.en.md와 함께 제거됨 — README로 통합)
!/README.html
!/README.en.html
